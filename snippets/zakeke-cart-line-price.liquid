{% assign customization_price = item.properties._configurationPrice | default: 0 | times: item.quantity %}
{% if customization_price > 0 %}
	{{ line_price | plus: customization_price | money }}
{% else %}
	{{ line_price | money }}
{% endif %}